---
import Layout from '../layouts/Layout.astro';
import postres from '../data/postres.json'
---
<Layout title="postres">
      <button onclick="location.href='/'">Volver al menu</button>
   <h1 class="text-2xl font-bold mb-4">Postres del Día</h1>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    {postres.map(item => (
      <div class="bg-white p-4 rounded shadow">
        <img src={item.imagen1} alt={item.nombre1} class="w-full h-48 object-cover mb-2" />
        <h3 class="text-xl font-semibold">{item.nombre1}</h3>
        <p class="text-green-700 font-bold">{item.precio1}</p>
      </div>
    ))}
  </div>
</Layout>