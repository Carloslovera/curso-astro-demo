---
import Layout from '../layouts/Layout.astro';
import bebidas from '../data/bebidas.json';
---
<Layout title=" bebidas">
      <button onclick="location.href='/'">Volver al menu</button>
   <h1 class="text-2xl font-bold mb-4">Bedidas del Día</h1>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    {bebidas.map(item => (
      <div class="bg-white p-4 rounded shadow">
        <img src={item.imagen3} alt={item.nombre3} class="w-full h-48 object-cover mb-2" />
        <h3 class="text-xl font-semibold">{item.nombre3}</h3>
        <p class="text-green-700 font-bold">{item.precio3}</p>
      </div>
    ))}
  </div>
</Layout>
